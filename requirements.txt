# RoadGuard - dependencies for Streamlit Cloud / GitHub deployment
# ----------------------------------------------------------------
# This is the file Streamlit Cloud auto-detects on a fresh build.
# Keep it in sync with requirements_pc.txt for local installs.

streamlit>=1.32
ultralytics>=8.1.0

# Use the *headless* OpenCV on Streamlit Cloud - the cloud runner has
# no display server, so plain opencv-python will fail at import time
# (libGL.so.1 missing). Headless is identical in API.
opencv-python-headless>=4.8

numpy>=1.24
pandas>=2.0
Pillow>=10.0

# Continuous Live (WebRTC) mode
streamlit-webrtc>=0.47
av>=10.0
aiortc>=1.6

# Vocal alerts (the toggle stays greyed out if gtts is missing)
gtts>=2.4

# PC -> Pi /announce HTTP callback (Pi speaker feature)
requests>=2.31

# torch / torchvision will be pulled in by ultralytics if not pinned.
# Pin a CPU build here so Streamlit Cloud doesn't try to fetch a CUDA
# wheel it can't run on.
torch>=2.1
torchvision>=0.16
